"""Offline deployment helpers for the Week 7 pricing models."""

# Keep package import lightweight. Import artifact_training explicitly so
# running it as a CLI does not import the same module twice.
